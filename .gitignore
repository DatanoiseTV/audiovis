# Rust build artifacts
/target
**/*.rs.bk
Cargo.lock.orig

# Browser-side wasm build output - regenerated by `tools/build-wasm.sh`, not
# a source file. (The `audiovis-wasm` crate compiles to `target/`; this dir
# holds the wasm-bindgen-produced JS glue + trimmed wasm the web UI loads.)
/webui/wasm-pkg

# Local long-term working memory (not for the repo)
TODO.md
STRUCTURE.md

# Local config / state captured at runtime
/state
/presets
/scripts
*.preset.local.json

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp

# Never commit private assistant instructions
CLAUDE.md
